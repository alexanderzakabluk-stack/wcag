{
  "name": "wcag-scan-api",
  "version": "1.0.0",
  "description": "WCAG 2.2 scanning API powered by Claude AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "node -e \"process.env.PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD||require('child_process').execSync('npx playwright install --with-deps chromium',{stdio:'inherit'});\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "nodemailer": "^8.0.1",
    "playwright": "1.58.2"
  }
}
